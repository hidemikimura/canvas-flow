# Cloudflare Workers（静的アセット）の設定。docs/ をそのままサイトのルートとして配信する。
# Workers Builds（Git 連携）でも `npx wrangler deploy` でも、この設定が出力先の決定に使われる。
name = "canvas-flow"
compatibility_date = "2026-09-17"

[assets]
directory = "./docs"
# 見つからない URL は docs/404.html を返す
not_found_handling = "404-page"
